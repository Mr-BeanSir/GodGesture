# GodGesture Codex Instructions

This file is the repository entry point for Codex.

Before analyzing, changing, testing, or committing project work:

1. Read `CLAUDE.md` in full and treat its project constraints as applying to Codex.
2. Follow the required reading order in `CLAUDE.md`, including `docs/PROJECT_STATUS.md`.
3. Inspect `git status --porcelain=v1` and recent Git history before editing.

Keep this file small. Shared project rules belong in `CLAUDE.md`; current implementation status belongs in `docs/PROJECT_STATUS.md`.
